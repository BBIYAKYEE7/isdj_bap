import { Feed } from 'instagram-private-api/dist/core/feed';
import { LocationFeedResponse, LocationFeedResponseMedia } from 'instagram-private-api/dist/responses';
export declare class LocationFeed extends Feed<LocationFeedResponse, LocationFeedResponseMedia> {
    id: string | number;
    tab: 'recent' | 'ranked';
    private nextMaxId;
    private nextPage;
    private nextMediaIds;
    protected set state(body: LocationFeedResponse);
    request(): Promise<LocationFeedResponse>;
    items(): Promise<LocationFeedResponseMedia[]>;
}
