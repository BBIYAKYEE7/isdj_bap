import { Feed } from 'instagram-private-api/dist/core/feed';
import { TimelineFeedReason, TimelineFeedsOptions } from 'instagram-private-api/dist/types/timeline-feed.types';
import { TimelineFeedResponse, TimelineFeedResponseMedia_or_ad } from 'instagram-private-api/dist/responses';
export declare class TimelineFeed extends Feed<TimelineFeedResponse, TimelineFeedResponseMedia_or_ad> {
    tag: string;
    private nextMaxId;
    reason: TimelineFeedReason;
    set state(body: any);
    request(options?: TimelineFeedsOptions): Promise<TimelineFeedResponse>;
    items(): Promise<TimelineFeedResponseMedia_or_ad[]>;
}
